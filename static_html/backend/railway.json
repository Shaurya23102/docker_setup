{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "DOCKERFILE",
    "dockerfilepath": "static_html/backend/Dockerfile"
  },
  "deploy": {
    "preDeployCommand": ["npm run db:migrate"],
    "startCommand": "uvicorn main:app --host 0.0.0.0 --port 8000"

  }
}